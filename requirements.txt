# Health Report V5.9.0 依赖
# 安装命令: pip3 install -r requirements.txt

# PDF生成
playwright>=1.40.0

# HTTP请求（OAuth2等可能需要）
requests>=2.28.0

# 语言检测（可选，用于改进的语言检测）
langdetect>=1.0.9

# 配置结构校验（validate_config.py 依赖）
jsonschema>=4.0.0

# Google认证（OAuth2邮件发送，可选）
google-auth>=2.22.0
# google-auth-oauthlib>=1.0.0

# 数据处理
# numpy>=1.24.0  # 如需要数值计算

# 注意: playwright需要安装浏览器
# 运行: python3 -m playwright install chromium
