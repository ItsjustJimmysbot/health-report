
## 2026-03-06 - Health Report V5.9.1 修复日志

今天完成了 health-report 仓库的多轮修复，最终推送到 GitHub: `ItsjustJimmysbot/health-report` (main 分支)

### 主要修复内容

**1. 版本统一与文档对齐**
- 将版本号统一为 V5.9.1（README、SKILL.md、config.schema.json、脚本 docstring）
- 同步更新 CHANGELOG.md，添加 [Unreleased] 小节

**2. 跨天 Workout 过滤修复**
- `generate_v5_medical_dashboard.py`: 添加 workout 日期过滤
- `extract_data_v5.py`: 同样添加日期过滤
- CI 测试同步更新

**3. Monthly CLI 参数校验**
- `generate_weekly_monthly_medical.py`: 添加 try/except 捕获非法输入
- 添加月份范围检查 (1-12)
- 国际化错误提示

**4. 站立时长字段对齐**
- 新增 `_active_energy_kcal()` 和 `_stand_hours()` 兼容函数
- 支持旧缓存字段：`active_energy_kcal`、`stand_time_min`
- 日报缓存写入 `apple_stand_hour` 字段
- 月报统计统一使用小时单位

**5. 语言阈值可配置**
- `utils.py`: 新增 `_get_language_ratio_thresholds()` 函数
- 支持通过 `analysis_limits` 配置 4 个语言比例阈值
- 默认：EN strict 0.15/warn 0.20；CN strict 0.30/warn 0.20
- 配置校验同步更新

**6. 模板预检查优化**
- 日报生成仅检查日报模板（避免周/月模板缺失阻塞日报）
- `verify_v5_environment.py` 使用 `get_template_path()` 复用真实回退逻辑

**7. 睡眠解析修复**
- `parse_sleep_data_unified()`: 支持秒级和毫秒级时间戳
- 新增 `_parse_sleep_ts()` 辅助函数

**8. OAuth2 配置保护**
- `setup_oauth2.py`: 保留用户原有 `provider_priority`，仅补齐缺失项

**9. 清理**
- 删除了意外提交到 GitHub 的 4 个指令文件

### 技术债务
- 无新增依赖
- 所有修改均为向后兼容
- CI 回归测试全部通过

### 验证状态
- ✅ Python 语法检查通过
- ✅ 配置 schema 校验通过
- ✅ 环境检查脚本通过
- ✅ 自定义回归测试通过

