## 2026-02-23 02:16 CST — Health report pipeline fixes (durable)

- User was dissatisfied with report UI quality and data integrity; requested strict adherence to V5.0 standardized flow.
- Confirmed and fixed multiple root-cause issues:
  1) Data source: moved daily generation to read **source Apple Health export JSON** (Health Data + Workout Data), not cache-only for critical fields.
  2) No-estimation rule: removed default/estimated fallbacks for critical metrics (flights_climbed, stand_time, basal_energy_burned, respiratory_rate). Missing real values now render as `--`.
  3) Sleep parsing: fixed extraction to use `sleep_analysis` fields (`sleepStart`, `asleep/deep/core/rem/awake`) with units in `hr`; do not depend on `qty` for this metric format.
  4) Workout energy units: fixed conversion from `kJ` to `kcal` (e.g., avoid showing 1209 kcal when source is 1209 kJ).
  5) Heart-rate chart: fixed clipping/overflow by adding chart bounds and conditional rendering; no curve shown on no-workout days.

- Standard entry integration:
  - Updated `scripts/generate_report_v5_ai.py` as the unified standard entry to support date-driven source-data mode with AI-analysis input from stdin JSON.
  - Maintained template-driven generation while ensuring unresolved placeholders are eliminated.

- Template/UI decisions:
  - Adopted `templates/DAILY_TEMPLATE_MEDICAL_V2.html` as the working dashboard-style daily template.
  - Unified Chinese typography to `Noto Serif SC` per user request for visual consistency.

- Important process lesson:
  - Do not reuse stale AI analysis text between reruns; regenerate analysis per date/run or enforce numeric-consistency checks before rendering.

- Output artifacts generated and validated during this session:
  - 2026-02-18 and 2026-02-19 daily reports (multiple iterations), including standard-entry reruns and previews.

---

## 2026-02-23 02:42 CST — Additional fixes

- Workout start/end time parsing fix:
  - Source Workout Data uses `start/end` fields (not `startDate/endDate`); updated extraction logic to prefer these fields.
  - Now renders as time range format (HH:MM-HH:MM) based on start + duration.

- Generated finalized 2.19 report:
  - Data: HRV 46.4ms, sleep 6.13h (deep 1.04h/core 3.54h/rem 1.54h), steps 1,993, distance 1.43km, active energy 127kcal.
  - No workout day (recovery day) → shows "无结构化运动" placeholder, no heart-rate curve.

- Verified consistency:
  - All analysis text regenerated per-run; numeric values match source data.
  - Font unified to Noto Serif SC across template.
